#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/* more headers goes there */
/**
 * main - Hexadecimal
 * Return: Always 0
 */
int main(void)
{
	int num;
	int alph;

	for (num = 48; num <= 57; num++)
	{
		putchar(n);
	}
	for (alph = 97; alph <= 102; alph++)
	{
		putchar(alph);
	}
	putchar('\n');
	return (0);
}
