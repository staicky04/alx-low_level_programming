#include "main.h"
#include <stdio.h>
/**
 * mul - multiplies two integers
 * @a: the first digit multiplied to 2nd digit
 * @b: second digit multiplied to first digit
 * Return: 1 if c is a capital letter, 0 otherwise
 */
int mul(int a, int b)
{
	return (a * b);
}
